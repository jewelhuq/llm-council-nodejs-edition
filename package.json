{
    "name": "llm-council-cloudflare",
    "private": true,
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "db:init:local": "wrangler d1 execute llm-council --local --file=schema.sql",
        "db:init:remote": "wrangler d1 execute llm-council --remote --file=schema.sql"
    },
    "devDependencies": {
        "wrangler": "^4"
    }
}
